/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['fingreen.org',"cdn.shopify.com",'media.istockphoto.com'],
    },
};

export default nextConfig;
